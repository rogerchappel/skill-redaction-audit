# Clean Skill

Use when an agent needs to draft a local-only note with placeholders.

## Side Effects

This skill only reads local fixtures and drafts output. It does not send email, publish content, or write to external systems.

## Approval

Ask for approval before any external action.

## Example

Use `user@example.com` and `sk_test_example` in public examples.
